# Dependencies for run_polls.py (GitHub Actions daily job + local runs)
pandas>=2.0,<3
requests>=2.28,<3
# Browser TLS fingerprint — many hosts return 403 to plain Python requests from datacenter IPs
curl-cffi>=0.7,<1
google-auth>=2.0,<3
google-api-python-client>=2.100,<3
numpy>=1.24,<3
lxml>=4.9,<7
# pandas read_html fallback parser when lxml finds no table (e.g. a Cloudflare
# challenge page) - without these the fallback ImportError crashes the run.
html5lib>=1.1,<2
beautifulsoup4>=4.11,<5

