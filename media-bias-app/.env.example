# Copy this file to .env and fill in your values.
# Never commit the .env file (it is in .gitignore).

# Required: Google Sheets API key (same key used by poll-tracker-app).
# Enable the Sheets API in Google Cloud Console and restrict the key to your domain.
VITE_GOOGLE_SHEETS_API_KEY=your_api_key_here

# Optional: override the default spreadsheet ID.
# VITE_GOOGLE_SHEETS_SPREADSHEET_ID=1RIqzrv_ViVWBqeXkM-rOAvusoXryyRFX5Xmu2S-uEw4

# Optional: for `npm run export-harmonized` / `export-residual-diagnostics` — must match a Website restriction on the same API key (default localhost Vite).
# Include both the tracker and this app path, e.g.:
# GOOGLE_SHEETS_REFERRER=https://YOUR_USERNAME.github.io/YOUR_REPO_NAME/media-bias/

# Set by CI to /<repo>/media-bias/ — leave blank for local dev (`npm run dev` uses `/`).
# VITE_BASE=/Elections_Polls/media-bias/
